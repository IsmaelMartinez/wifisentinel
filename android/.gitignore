*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# The Gradle wrapper (gradlew, gradlew.bat, gradle/wrapper/*) is committed
# so CI and fresh clones build without a local Gradle install. The jar is
# pinned alongside gradle-wrapper.properties (Gradle 8.11.1 + SHA-256).

# Build outputs
/app/build/
/app/release/
/app/debug/
