# kotlinx-serialization keeps its serialisers via the Kotlin companion — the
# default Compose rules are sufficient, so this file intentionally stays empty
# for the spike. Add app-specific keep rules here once we ship a release build.
